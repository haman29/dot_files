設定ファイル置き場

# contents
.dir_colors    lsした時の色つけ
.screenrc      screen用
.vimrc         vim用
_vimperatorrc  vimperator用(firefox plugin)

# use(vim)
$ git https://github.com/hamaan29/dot_files
$ ln -s .vimrc dot_files/.vimrc
$ ln -s .vim dot_files/.vim
$ git clone http://github.com/gmarik/vundle.git ~/.vim/bundle/vundle/
$ vim
 # :BundleInstall
 # とタイプ(.vimrcに記述したpluginがインストールされる)
